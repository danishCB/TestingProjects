language: java
jdk:
- oraclejdk8
install:
- sudo apt-get install -u lib32stdc++6 lib32z1
- chmod +x gradlew
before_script:
- mkdir -p shippable/testresults
script:
- chmod +x gradlew
after_script:
- cp -r build/test-results/*
notifications:
  email:
    recipients:
    #- danish.hassan@citrusbits.com